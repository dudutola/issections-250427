<h1>My Sections</h1>

<%= link_to "+ New Section", new_section_path, class: "btn" %>

<div class="sections">
  <% @sections.each do |section| %>
    <div class="section_card">
      <h2><%= section.name %></h2>
    </div>
  <% end %>
</div>
